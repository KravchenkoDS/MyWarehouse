default: &default
  adapter: postgresql
  encoding: unicode
  username: 'sws_user'
  password: 'qwerty'
  host: localhost
  port: 5432
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  <<: *default
  database: sws_development

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: sws_test

production:
  <<: *default
  database: sws_production
